{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:wasm": "wasmbridge-net sync --release",
    "sync:wasm:debug": "wasmbridge-net sync --debug",
    "predev": "npm run sync:wasm:debug",
    "dev": "vite",
    "prebuild": "npm run sync:wasm",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.0.0",
    "wasmbridge-net": "^0.1.5"
  }
}
