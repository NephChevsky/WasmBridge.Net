bin/
obj/
artifacts/
*.user
.vs/
.vscode/
Nuget.Config
node_modules/

# wasmbridge-net's CLI entrypoint lives in a "bin/" folder too (npm convention), but the
# blanket .NET "bin/" ignore above would otherwise also match and hide it from git/npm.
!wasmbridge-net/bin/
!wasmbridge-net/bin/**
