name: Publish WasmBridge.Net.Sdk to NuGet

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write  # enable GitHub OIDC token issuance for this job
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Test
        run: dotnet test WasmBridge.Sdk.Tests/WasmBridge.Sdk.Tests.csproj -c Release

      - name: Restore
        run: dotnet restore WasmBridge.Sdk/WasmBridge.Sdk.csproj

      - name: Pack
        run: dotnet pack WasmBridge.Sdk/WasmBridge.Sdk.csproj -c Release -o ./nupkg

      # Get a short-lived NuGet API key
      - name: NuGet login (OIDC → temp API key)
        uses: NuGet/login@v1
        id: login
        with:
          user: Neph # your nuget.org username (profile name), NOT your email address

      - name: Push to NuGet
        run: dotnet nuget push "./nupkg/*.nupkg" --api-key ${{ steps.login.outputs.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
